export const metadata = {
  title: "Admin Instakash",
  description: "Administrador de datos de Instakash"
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
